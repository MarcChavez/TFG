using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class WanderingGroup : MonoBehaviour
{
    // Start is called before the first frame update
    int destPoint;
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
